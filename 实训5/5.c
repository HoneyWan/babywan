// 数组中插入一个数
#include <stdio.h>
void main()
{
    int a[7] = { 1, 2, 3, 4, 5, 6 };
    int n;
    printf("Enter a number:");
    scanf("%d", &n);
    if ( n > a[5] )
    {
        a[6] = n;
    }
    else if ( n > 6 / 2)
    {
        
    }
}